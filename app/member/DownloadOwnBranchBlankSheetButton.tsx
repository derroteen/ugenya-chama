// Deprecated: the "Download Blank Sheet" feature was removed from the member view
// (blank sheets are now admin-only, downloaded from the branch page instead).
// This component is no longer imported anywhere - safe to delete entirely with:
//   git rm app/member/DownloadOwnBranchBlankSheetButton.tsx
// Left as an inert stub for now so a leftover copy of this file never breaks
// `next build`'s type check again.
export default function DownloadOwnBranchBlankSheetButton() {
  return null;
}
